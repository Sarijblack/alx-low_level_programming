#include "main.h"
#include <stdio>

/*
 * main - Entry point
 * prints num from 0 to 9
 * Return: Always succesd
 */
int main(void)
{
	char a;
	for (a = '0'; a <= '9'; a++)
	{
		_putchar(a);
	}
		_putchar('\n');

		return (0);
}

