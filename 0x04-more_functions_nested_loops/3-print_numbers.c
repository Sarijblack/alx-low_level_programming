#include "main.h"
#include <stdio>

/*
 * main - check the code
 * prints num from 0 to 9
 * Return: Always succesd
 */
void print_numbers(void)
{
	char a;

	for (a = '0'; a <= '9'; a++)
	{
		_putchar(a);
	}
		_putchar('\n');

		return (0);
}

